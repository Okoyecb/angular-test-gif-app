import React from "react"
import { shallow } from "enzyme"
import GifItem from "../components/GifItem"

describe("GifItem component tests", () => {
	it("should check that <GifItem /> renders correctly", () => {
		//after mapping over the gifItem props, "<GifItem />" should render correctly
		expect(variable.find().toEqual)
	})

	it("should fire the onGifSelect method onClick", () => {
		//should check that the state is set to "modalIsOpen: true" and "selectedGif: gif" onClick
		// modalIsOpen must be true
	})
})
