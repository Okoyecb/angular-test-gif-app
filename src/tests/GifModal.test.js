import React from "react"
import { shallow } from "enzyme"
import GifModal from "../components/GifModal"

describe("GifModal component tests", () => {
	it("should check that props are received", () => {
		//not sure what to put in here
	})
})
