import React from "react"
import { shallow } from "enzyme"
import SearchBar from "../components/SearchBar"

describe("SearchBar component tests", () => {
	it("should check if a search term has been entered", () => {
		// SearchBar should generate results at 1st key pressed
	})

	it("should fire the onInputChange method", () => {
		//onChange triggers onInputChange
	})
})
